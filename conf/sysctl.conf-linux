#
# $Id$
#
# For correct reception of the dvb-s2 data, the recommendation is to
# add the line given below to "/etc/sysctl.conf" so that it takes
# effect every time that the computer boots. 
#

# Maximum udp buffer size (5 MB)
net.core.rmem_max = 5242880

# Maximum number of fragments in queue (0 => no limit)
net.ipv4.ipfrag_max_dist = 0
