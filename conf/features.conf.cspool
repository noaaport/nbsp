#
# This file exists only for documentation purposes because the cspool
# has never been operational.
#
# cspool configuration -
#
# set feature(spooltype) 4;  # A file-backed bdb spool (cspool)
# set feature(spooltype) 5;  # A (shared) memory based cspool
#
# If the cache spool (4) or (5) is enabled, then consult the
# file "cspoolbdb.conf" in order to configure the filters to use the
# spool cache.
