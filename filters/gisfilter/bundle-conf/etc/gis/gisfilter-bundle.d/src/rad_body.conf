
if {[info exists bundleplist(rad_${_name})]} {
    set _plist $bundleplist(rad_${_name});
} else {
    set _plist $bundleplist(rad_default);
}

set p [join ${_plist} "|"];
rad_bundle_add "shp" "wct_defaults.xml" \
 	"-(${p})([join [nbsp::radstations::bystate ${_slist}] "|"])";
