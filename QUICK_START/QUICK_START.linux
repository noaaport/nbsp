$Id$

QUICK_START.linux

- Centos

	rpm -i nbsp-<version>.rpm

- Debian

	dpkg -i nbsp-<version>.deb

It will be configured for automatic start/stop at boot/shutdown time.

It can also be started/stoped manually by executing

	/etc/init.d/nbspd stop
	/etc/init.d/nbspd start
